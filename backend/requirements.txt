# Core
pydantic>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
PyYAML>=6.0

# Web
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
python-multipart>=0.0.9  # required for UploadFile / Form endpoints (asset upload)

# LLM
openai>=1.0.0

# Feishu long-connection (official SDK — required for WebSocket event receiving)
lark-oapi>=1.4.0
