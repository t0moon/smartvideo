# SmartVideo — 简历项目描述

> AI 产品经理视角 | 用于简历 / 作品集 / 面试

---

## 项目概览

**项目名称** SmartVideo — 企业级 AI 视频生产平台

**角色** AI 产品经理（独立设计 & 主导迭代）

**技术栈** Python / FastAPI / React 19 / LangGraph / FFmpeg / DeepSeek v4 / Kling 2.5 Turbo / 飞书开放平台

**项目定位** 面向企业市场部门的 AI 视频自动化生产平台，通过 Agent + Workflow + Human-in-the-Loop 实现从 brief 到成片的完整闭环。

**一句话** 不是"写 prompt 出视频"的工具，而是能替代视频团队中「需求理解→分镜→素材准备→拍摄→审核→合成→发布」全流程的 AI 生产系统。

---

## 一句话介绍（适合简历首行）

```
独立设计并主导开发了企业级 AI 视频生产平台 SmartVideo，
通过 Agent 工作流 + 4 节点人机协同 + YAML 可配置流水线，
实现了从 brief 到成片的自动化闭环，已在飞书 IM 中完整跑通。
```

---

## 核心成果（简历 bullet）

- **产品闭环设计**：从 0 到 1 设计了覆盖 Requirement → Storyboard → Asset Prep → Scene Gen → Video Gen → Stitch → Publish 的 7 阶段视频生产流水线，定义了 4 个关键人工审批节点，实现 AI 生成与人工决策的精准分工。

- **Agent 架构决策**：放弃业界常见的多 Agent 协同方案，采用「单 LeadAgent + 阶段化 Skill 注入」架构——通过在不同流水线阶段动态切换 system prompt（SKILL.md）来改变 Agent 行为，减少 Agent 间通信开销，降低调试复杂度。

- **零配置交互体验**：主导飞书 IM 审批体验从「交互式卡片」到「纯文本对话」的重构，砍掉了对飞书后台 card.action.trigger 事件订阅的依赖，用户只需回复「批准」「驳回 改XX」等自然语言即可完成全部审批操作，部署门槛降低至零配置。

- **可配置流水线引擎**：推动流水线从硬编码状态机重构为 YAML 驱动的可配置引擎，通过纯 YAML 文件定义工作流阶段、SKILL 映射和暂停规则，实现了不改代码即可新增视频生产模板（产品广告/抖音短视频/数字人口播）。已产出 3 套视频类型模板。

- **领域知识注入**：编写了广告分析 Skill（品牌解码 + 平台格式感知 + 品类反模式，覆盖 6 种品牌人格、5 个内容平台、7 个消费品类）和 589 行爆款商业广告脚本生成方法论，将广告领域的行业知识结构化为可复用的 Agent prompt 模板。

- **素材资产化**：设计了素材上传→资产登记→跨项目复用的资产体系，支持产品图/模特图/BGM 的上传与自动注入生成链路（参考图接入 Kling 图生视频、BGM 自动混入成片合成）。

- **反馈闭环**：在 4 个审批节点实现了「驳回意见 → LLM 局部重写」的产品闭环——用户驳回时携带的修改意见（如「驳回 改成暖色调」）直接回流为 LLM 的多轮对话上下文，Agent 基于自己上一轮输出做增量修改而非全盘重来。

---

## 关键数据（选填，可用于简历量化）

```
- 全流程节点：7 个生产阶段 + 4 个人工审批暂停点
- 支持渠道：飞书 IM 原生对话 + Web 平台
- 可用视频模板：3 套（产品广告 / 抖音短视频 / 数字人口播）
- 视频生成：Kling 2.5 Turbo 单条 5s 约 40-50s
- 迭代周期：6 天完成 17 次产品迭代
- Git 历史：4 个里程碑 commit
```

---

## 面试故事线（STAR 结构，供面试口述）

**S (Situation)**
市面上 AI 视频产品普遍是「prompt in, video out」的黑盒模式，缺少完整的视频生产流程、人工审核节点、资产沉淀能力，且无法与企业 IM 集成。企业市场部门依然需要大量人工介入视频制作流程。

**T (Task)**
我需要设计一个面向企业用户的 AI 视频生产平台，让非专业视频制作者（市场/运营人员）能够通过自然语言快速产出可发布质量的视频，并且整个流程可控、可审核、可复用。

**A (Action)**
1. 首先梳理了视频生产的完整业务流（需求理解→分镜→素材→拍摄→后期→审批→发布），据此设计了 7 阶段流水线架构。
2. 决定在 4 个关键节点插入人工审批（需求/分镜/资产/成品），因为这是真实企业视频制作的不可省略环节。
3. 在 Agent 架构上，放弃了多 Agent 方案，选择了更简洁的单 Agent + prompt 切换方案，牺牲了一些灵活性换来了可维护性。
4. 把飞书 IM 作为第一交互入口进行深度打磨——从卡片按钮到纯文本对话的迭代，让用户零学习成本。
5. 推动流水线引擎从硬编码到 YAML 可配置，为多模板扩展铺路。

**R (Result)**
- 系统从飞书发一条消息到产出完整成片（含配音/字幕/BGM），端到端闭环跑通。
- 用户交互只有两种动作：发需求、说「批准」或「驳回 改XX」，无任何按钮点击。
- 3 套视频模板通过纯 YAML 配置定义，新增模板零代码改动。
- 项目成为个人作品集中最能体现「AI 产品架构能力 + 用户体验打磨」的案例。

---

## 适配不同岗位的可选版本

### 投「AI 产品经理」— 侧重产品架构 & 用户洞察

```
独立设计 SmartVideo 企业级 AI 视频生产平台，覆盖 7 阶段流水线 + 4 节点人机协同。
关键决策：放弃多 Agent 协同、选择单 Agent + prompt 切换架构；
将飞书审批体验从卡片重构为纯文本对话，部署门槛降为零。
核心能力：B 端产品架构 / HITL 设计 / LLM 交互范式探索。
```

### 投「AIGC 产品经理」— 侧重 AI 应用落地

```
从 0 到 1 设计 AIGC 视频生产闭环——非 prompt-to-video 黑盒，
而是完整覆盖需求理解→分镜→生视频→配音字幕→审批→发布的 7 段流程。
将广告行业知识（品牌解码/平台感知/品类反模式）结构化为可复用的 Agent prompt 模板。
LLM + 视频模型 + FFmpeg 工具链的三层调度：DeepSeek 做内容理解，Kling 做视频生成，FFmpeg 做后期合成。
```

### 投「AI 平台/B 端产品」— 侧重系统设计

```
设计 YAML 驱动的可配置工作流引擎（兼容 3 套视频模板，新增模板零代码改动）。
建立素材资产体系（上传→登记→跨项目复用），支持参考图接 Kling 图生视频、BGM 自动混入合成。
双存储系统（文件系统 + SQLite）的三层状态管理（ProjectStage / WorkflowState / ReviewService）。
```
